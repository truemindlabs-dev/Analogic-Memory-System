# ─────────────────────────────────────────────
# Analogic Memory System - Environment Variables
# Copy this to .env and fill in your values
# ─────────────────────────────────────────────

# Server
PORT=8000
ENV=production

# PostgreSQL (Railway provides this automatically)
DATABASE_URL=postgresql://user:password@localhost:5432/analogic_memory

# API Authentication
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
API_TOKEN=your-secure-api-token-here

# Encryption
# Generate 32-byte hex key: python -c "import secrets; print(secrets.token_hex(32))"
MASTER_ENCRYPTION_KEY=your-64-char-hex-key-here
SECRET_PASSPHRASE=your-fallback-passphrase-here
KEY_SALT=your-unique-salt-here

# Backup Storage
BACKUP_DIR=/var/backups/analogic_memory
MAX_LOCAL_BACKUPS=48

# S3 Backup (optional - leave blank to disable)
S3_BACKUP_BUCKET=
S3_BACKUP_PREFIX=analogic-memory-backups/
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1

# CORS (comma-separated origins, or * for all)
ALLOWED_ORIGINS=https://your-frontend.railway.app,https://your-app.base44.com

# Trusted Hosts (comma-separated, leave blank to disable)
TRUSTED_HOSTS=
